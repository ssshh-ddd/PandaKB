#
# 电池电量报告间隔时间单位是秒
# (バッテリー残量レポートの間隔は秒単位)
CONFIG_ZMK_BATTERY_REPORT_INTERVAL=60
# 增加键盘消抖
# (キーボードデバウンス機能を追加)
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5
# 增加键盘的 BLE 无线电的发射功率，用解决延迟和左右通讯不佳的问题
# (キーボードのBLE無線機の送信出力を上げることで、遅延や左右間の通信不良の問題を解決)
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
# 更改键盘默认显示名称
# (デフォルトのキーボード表示名を変更)
CONFIG_ZMK_KEYBOARD_NAME="PandaKB_Corne"
# 用于解决 Windows 中与电池报告相关的错误
# (Windowsにおけるバッテリーレポート関連のエラーを解決するために使用)
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

# oled相关配置
# (OLED関連構成)
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y

# 休眠相关配置
# (休止状態関連の設定)
# Milliseconds of inactivity before entering idle state	(10 minute)
CONFIG_ZMK_IDLE_TIMEOUT=600000
# Enable deep sleep support	
CONFIG_ZMK_SLEEP=y
# Milliseconds of inactivity before entering deep sleep (15 minutes)
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

# zmk studio相关配置
# (ZMK Studioの設定)
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n
CONFIG_ZMK_STUDIO_LOCK_ON_DISCONNECT=n

# rgb相关配置
# (RGB関連の設定)
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_WS2812_STRIP=y
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_USB=y

# 键盘进入空闲状态时关闭RGB
# (キーボードがアイドル状態のときにRGBをオフ)
CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_IDLE=y
CONFIG_ZMK_RGB_UNDERGLOW_EFF_START=3
CONFIG_ZMK_RGB_UNDERGLOW_BRT_MAX=60
CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=n
CONFIG_ZMK_RGB_UNDERGLOW_BRT_START=60
